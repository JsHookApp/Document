# base64

Base64 encoding and decoding for strings and byte arrays.

## base64.encode(data)

`Parameters`:

`data`: string

`Returns`: string

## base64.encodeBytes(data)

`Parameters`:

`data`: byte[]

`Returns`: byte[]

## base64.decode(data)

`Parameters`:

`data`: string

`Returns`: string

## base64.decodeBytes(data)

`Parameters`:

`data`: byte[]

`Returns`: byte[]
