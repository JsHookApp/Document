# dialog

Common dialog utilities.

## dialog.input(title,callback,content)

`Parameters`:

`title`: string

`callback`: function

`content`: string

Example:

```javascript
dialog.input('Title', {
    ok: function (res) {
        console.log(res);
    },
    cancel: function () { }
}, 'Default value');
```
