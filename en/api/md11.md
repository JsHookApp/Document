# storage

Local data storage. Data is stored in the hooked app's private directory.

## storage.get(key)

`Parameters`:

`key`: string

`Returns`: string

## storage.set(key,data)

`Parameters`:

`key`: string

`data`: string

## storage.del(key)

`Parameters`:

`key`: string

## storage.clear()
